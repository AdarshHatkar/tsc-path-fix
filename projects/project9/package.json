{
  "private": true,
  "scripts": {
    "build": "npm i && tsc && tsc-path-fix --resolve-full-paths",
    "start": "npm run build && node ./dist/index.js"
  },
  "dependencies": {
    "buffercursor.ts": "1.0.3"
  },
  "devDependencies": {
 
    "tsc-path-fix": "file:../../"
  }
}
