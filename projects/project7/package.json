{
  "private": true,
  "scripts": {
    "build": "npm i && tsc && tsc-path-fix",
    "start": "npm run build && node ./dist/project7/src/index.js"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "tsc-path-fix": "file:../../"
  }
}
