{
  "private": true,
  "scripts": {
    "build": "tsc && tsc-path-fix --resolve-full-paths",
    "start": "npm run build && node ./dist/main.js"
  },
  "devDependencies": {
 
    "tsc-path-fix": "file:../../"
  }
}
